Next JS, Express JS, GraphQL starter kit.

Server:
  - Node JS,
  - Express JS,
  - Apollo - Server
  - Mongoose
  
Web:
  - React JS
  - Next JS
  - Apollo - Client
  - Ant Design
  - Styled Components
  
For run Server:

```cd server && yarn start```

For run Web:

```cd web && yarn dev```
